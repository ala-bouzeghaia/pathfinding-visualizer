const SQUARE_SIZE = 30;
const NUMBER_OF_ROWS = 20; /* Math.floor(
  (window.innerHeight * 0.92 - 16) / SQUARE_SIZE
); //20; */
const NUMBER_OF_COLS = 27; //Math.floor((window.innerWidth * 0.85) / SQUARE_SIZE); //27;

const START_NODE_ROW = 8;
const START_NODE_COL = 7;
const FINISH_NODE_ROW = 8;
const FINISH_NODE_COL = 25;

export {
  NUMBER_OF_COLS,
  NUMBER_OF_ROWS,
  SQUARE_SIZE,
  START_NODE_COL,
  START_NODE_ROW,
  FINISH_NODE_COL,
  FINISH_NODE_ROW,
};
